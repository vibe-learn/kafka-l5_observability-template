package main

import "testing"

// TestPlaceholder is the stable seed test for the `l5_observability` homework template.
// The test SKIPS by default so a freshly-cloned repo's CI is green out of the box.
// Replace `t.Skip(...)` with real assertions when you implement the lesson task.
func TestPlaceholder(t *testing.T) {
    t.Skip("TODO: replace this skip with real assertions per README.md")
}
